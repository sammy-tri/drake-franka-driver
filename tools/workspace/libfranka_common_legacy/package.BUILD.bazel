# -*- mode: python -*-
# vi: set ft=python :

package(default_visibility = ["//visibility:public"])

cc_library(
    name = "libfranka_common_legacy",
    hdrs = glob(["include/research_interface/**/*.h"]),
    includes = ["include"],
)
